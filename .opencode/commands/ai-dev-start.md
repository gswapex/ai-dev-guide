---
description: 从项目库或明确目标快速开始
---

Load and use the installed `ai-dev-guide` skill.

Do not run the full idea-derivation guide by default.

In Chinese:

- confirm whether the user already has a target project
- if not, let the user choose from the installed project library
- if course background is still unclear, clarify it first
- once the target is confirmed, read the relevant `projects/*` package
- prepare to initialize project docs after the target is confirmed
- do not use `examples/*.md` as the default input source for this command

Follow the installed skill's `SKILL.md` and `AGENTS.md`.
